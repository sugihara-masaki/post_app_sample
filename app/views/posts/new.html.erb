<h1>新規投稿</h1>
<%= form_with(model: @post) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.file_field :image %>
  <%= f.rich_text_area :content %>

  <%= f.submit '投稿', class: 'btn btn-primary' %>
<% end %>
